# Screenshot Tool Configuration
# Copy this file to .snap.env and fill in your credentials

# Base URL for the application
SNAP_BASE_URL=http://localhost:5000

# Login configuration (required for authenticated pages)
SNAP_LOGIN_PATH=/login
SNAP_USER_SEL=#email
SNAP_PASS_SEL=#password
SNAP_SUBMIT_SEL=button[type=submit]

# Credentials (⚠️ NEVER commit .snap.env to git!)
SNAP_USERNAME=admin
SNAP_PASSWORD=admin123

# Optional: Post-login verification selector
# SNAP_POSTLOGIN_SEL=#sidebar

# Optional: Custom viewports (comma-separated WxH)
# SNAP_VIEWPORTS=1440x900,1280x800,1024x768,390x844

# Optional: Output directory
# SNAP_OUT_DIR=snapshots

# Optional: State file location
# SNAP_STATE_FILE=tools/snapshots/state.json

# Optional: Retry settings
# SNAP_RETRIES=2
# SNAP_TIMEOUT_MS=20000
